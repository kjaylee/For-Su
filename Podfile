# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'for su' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  # Don't care inhibit warning.
  inhibit_all_warnings!

  # Pods for for su
  pod 'Hero'
  pod 'IGListKit'
  pod 'netfox'
  target 'for suTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'for suUITests' do
    # Pods for testing
  end

end
